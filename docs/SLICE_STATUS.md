# Slice status

One line per slice as it merges. Newest at the bottom.

## Slice #1 — Project foundation (branch `cursor/issue-1-foundation-2c32`)
**In:** Vendored NDI Standard SDK linked into target. Bundle ID, signing team, iPad-only / landscape / dark / Stage Manager opt-out, iPadOS 17 deployment target, `NSLocalNetworkUsageDescription` + `NSBonjourServices=_ndi._tcp`. Bridging header + `NDIRuntime` ObjC++ wrapper around `NDIlib_initialize/destroy` (refcounted, started on scene-active, stopped on scene-background). `ContentView` shows NDI version + CPU-supported flag instead of generic Hello World, proving the SDK linked.
**Deferred / HITL:** Archive + sign + run on physical iPad (no Xcode in CI VM). First-launch local-network prompt visible (HITL). All AFK source code is in.

## Slice #2 — Single NDI source on screen (branch `cursor/issue-2-single-ndi-source-2c32`)
**In:** `NDIReceiver.{h,mm}` wrapping `NDIlib_recv_*` + `NDIlib_FrameSync` with the `connect/disconnect/latestFrame` shape. Frames cross from C NDI into a `CVPixelBuffer` zero-copy via `CVPixelBufferCreateWithBytes`, with a heap-allocated release ref-con that holds the recv+framesync alive past disconnect so `NDIlib_framesync_free_video` always runs against a live framesync. UYVY (`kCVPixelFormatType_422YpCbCr8`) and BGRA (`kCVPixelFormatType_32BGRA`) are mapped; other FourCCs log + drop. `NDIDiscoveryFirstSource.{h,mm}` is a transient first-source helper (slice #3 supersedes it). `Metal/MetalPreviewView.swift` is a `UIViewRepresentable` over `MTKView` that pulls `latestFrame()` per draw and renders aspect-fit via `CIContext` + `CIRenderDestination`. `ContentView` body is now the live preview with a "Searching for NDI sources…" overlay until the first source connects. Bridging header exposes `NDIReceiver` and `NDIDiscoveryFirstSource`.
**Deferred / HITL:** Build + run on physical iPad against NDI Test Patterns is HITL (no Xcode in CI VM). Foreground/background leak verification is HITL. Hardcoded "first discovered source" connection — slice #3's `NDIDiscovery` + picker UI replaces both the helper and this auto-connect path. Core Image is the chosen render path for now; slice #4's hand-rolled UYVY-sampling Metal compositor will replace it. Non-UYVY/non-BGRA FourCCs are dropped this slice; widening fourcc support is slice #4 too.

## Slice #3 — NDI source picker (auto-discovery + manual URL) (branch `cursor/issue-3-source-picker-2c32`)
**In:** `NDI/NDIDiscovery.{h,mm}` is the durable replacement for `NDIDiscoveryFirstSource` — singleton (`+shared`) with a serial background find loop over `NDIlib_find_wait_for_sources` + `NDIlib_find_get_current_sources`, an `NDISource` value type (`name`, `urlAddress`, `isEqual:`/`hash` defined), `os_unfair_lock`-guarded snapshot, and a main-queue `onSourcesChanged` callback. The find loop owns the find-instance lifetime end-to-end (created in `start`, destroyed inside the loop after `_stopRequested` flips) so `find_destroy` never races the loop's `find_get_current_sources`. `Models/SourceSelection.swift` is the `@MainActor @Observable` Left/Right selection holder with `swap()`. `Models/DiscoveredSources.swift` bridges the ObjC singleton's callback into Observation. `Views/SourcePickerSheet.swift` shows live-updating discovered sources with a manual `ndi://host[:port]/StreamName` entry row that constructs an `NDISource` whose `urlAddress` is `host:port` (default port 5961 if absent) and `name` is `Manual (StreamName)`. `Views/URLParseHelpers.swift` extracts the URL parser as a pure helper for testability — handles IPv6 brackets, percent-decoding, and bare names with spaces. `Views/TopBar.swift` is the always-visible top row: two source dropdowns, a Swap button (disabled when either side is nil), and a disabled gear placeholder. `ContentView.swift` rewritten: `MetalPreviewView` underneath, `TopBar` over the top, an "Pick a source from the top bar" overlay until Left is selected, and `.onChange(of: selection.leftSource)` re-`connect`s / `disconnect`s the receiver. Bridging header now imports `NDIDiscovery.h` (alphabetized: NDIDiscovery, NDIReceiver, NDIRuntime). `NDIDiscoveryFirstSource.{h,mm}` deleted. `stereondiTests/URLParseHelpersTests.swift` covers the spec'd parse cases (host[:port]/name, percent-encoded + bare-space names, rejected schemes, missing parts, leading/trailing whitespace).
**Deferred / HITL:** Build + run on physical iPad with two NDI sources is HITL (no Xcode in CI VM); manual integration test is "switch between two NDI sources via the picker, confirm preview tracks the selection". Right pipeline is unbuilt — Right selection is captured and shown in the top bar but does not affect rendering; `FramePairer` + dual-receiver wiring is a later slice. Auto-hide top bar after 3s is deferred to slice #13. Settings gear is a disabled placeholder (slice #10 wires it). Manual-URL default port is `5961` (NDI's typical first port); a future slice can surface it as user-editable if operators hit non-default deployments.

## Slice #4 — Stereo SbS composite + compositor + golden-image test infra (branch `cursor/issue-4-stereo-sbs-2c32`)
**In:** Two-receiver pipeline now lights up the second eye. `Metal/FrameTypes.swift` defines the `VideoFrameSource` / `VideoFrameReceiving` protocol seams (with `nonisolated` requirements so ObjC `NDIVideoFrame` / `NDIReceiver` conform without isolation friction) and the `StereoFramePair` value type (`@unchecked Sendable` — backing `CVPixelBuffer` is read-only post-create). `Metal/FramePairer.swift` is `@MainActor`, owns a `CADisplayLink` on `.main`/`.common`, and has a public `tick(hostTime:)` so tests can drive it without Metal/UIKit. The display link uses a weak-owner trampoline (`DisplayLinkProxy`) so the pairer isn't pinned alive by `CADisplayLink`'s strong-target retain. `Metal/StereoCompositor.swift` + `Metal/Compositor.metal` implement zero-HIT SbS only: shared `sbs_vertex`, format-specific `sbs_fragment_bgra` and `sbs_fragment_uyvy` (UYVY decoded from `.gbgr422` sampling + BT.709-limited YCbCr→RGB math), per-side aspect-fit into a 960×1080-proportional half via a triangle-strip quad. Pipelines are cached by `(SourceKind, target pixelFormat)` so the same compositor handles both the MTKView's `.bgra8Unorm` and the test target's offscreen `.bgra8Unorm`. `CVMetalTexture` wrappers are pinned via `addCompletedHandler` so the IOSurface backing isn't recycled before the GPU samples it. `Metal/MetalPreviewView.swift` is rewritten — the Coordinator hooks `pairer.onTick` to cache the latest pair + `setNeedsDisplay` the MTKView (which is `isPaused = true, enableSetNeedsDisplay = true`), and `draw(in:)` calls `compositor.render(...)`. `ContentView.swift` now owns two `NDIReceiver`s, a `FramePairer`, and a `StereoCompositor`; left/right `selection.onChange` drive the respective receivers. Test target gains: `HelperToken.swift` (Bundle anchor for Swift Testing free functions), `GoldenImage.swift` (load / write / per-pixel-channel-diff comparison with configurable tolerance), `MetalRenderUtilities.swift` (1920×1080 render-target, 960×540 BGRA gradient pixel buffer, texture readback to `CGImage`, `StubVideoFrame` mock), `StereoCompositorGoldenTests.swift` (`zeroHitSbSGradientPair` — renders red-left / blue-right gradient pair, compares against bundled `Goldens/sbs_zero_hit_gradient.png` at 0.02 tolerance, or rewrites the PNG into `stereondiTests/Goldens/` when `STEREONDI_UPDATE_GOLDENS=1`), `FramePairerTests.swift` (scripted `FakeReceiver` exercising both-sides / left-only / nil-nil / `setReceivers` swap). `stereondiTests/Goldens/README.md` documents the update-goldens workflow and the file-system-synchronized auto-include behavior. No bridging-header / project-file changes.
**Deferred / HITL:** Reference PNG `Goldens/sbs_zero_hit_gradient.png` is **not** committed because Metal isn't available on this Linux CI VM — the user must run `STEREONDI_UPDATE_GOLDENS=1 xcodebuild test -scheme stereondi …` once on a Mac to seed it (see `stereondiTests/Goldens/README.md`); the "test passes via `xcodebuild test`" AC item is HITL-blocked on this single seeding step. Build + run on physical iPad with two live NDI sources confirming the SbS composite is HITL. UYVY sampling format (`.gbgr422` vs `.bgrg422`) is a HITL-verify item — if iPad shows swapped chroma, flip the format binding in `StereoCompositor.drawSide` (shader math is unchanged); see the inline comment. HIT/crop math, anaglyph, channel-test, single-source partial-preview placeholder, and the matching golden tests are explicitly later slices (#6 / #7 / #13). The pairer fires a callback even when both eyes are nil; the compositor renders solid black, which is the slice #4 contract. Resolution-mismatch scaling and stall/zombie detection are slice #11.

## Slice #5 — NDI sender: full-bandwidth SbS output to Quest (branch `cursor/issue-5-ndi-sender-2c32`)
**In:** Output side of the pipeline lights up. `NDI/NDISender.{h,mm}` wraps a single `NDIlib_send_*` instance — `-startWithName:groups:` (idempotent restart), `-sendUYVYFrame:width:height:stride:frameRateNumerator:frameRateDenominator:` (synchronous `NDIlib_send_send_video_v2`, serialized on `os_unfair_lock` so the GPU completion thread + main-thread stop never race), `-stop` (`NDIlib_send_destroy` + matched `[NDIRuntime stop]`). UTF-8 stream-name and groups buffers are owned by the sender (NUL-terminated NSData copies kept alive for the lifetime of the send instance — the SDK doesn't deep-copy the create-struct strings). `clock_video = clock_audio = false` because the FramePairer's `CADisplayLink` is already our clocking source. Bridging header now imports `NDISender.h` (alphabetized: Discovery, Receiver, Runtime, Sender). `Metal/UYVYEncoder.{swift,metal}` is the new compute pipeline — kernel `bgra_to_uyvy_bt709` reads two adjacent BGRA texels per thread, computes per-pixel BT.709-limited Y, averages chroma, and writes packed (U, Y0, V, Y1) bytes into a `device uchar*` buffer at `(y*bytesPerRow + x*4)`. Threadgroup is 16×16 over a `(width/2 × height)` grid via `dispatchThreads` (non-uniform thread groups are universally available on the iPad Pro M-series floor). UYVY destination is an `MTLBuffer` (not a texture) because Metal's `bgrg422`/`gbgr422` formats are read-only on Apple silicon. Even-source-width invariant is a `precondition`. `Metal/StereoCompositor.swift` gains an offscreen 1920×1080 `.bgra8Unorm` render target (`.private` storage, `.renderTarget|.shaderRead` usage, lazily allocated and reused) and a `renderForSender(pair:commandBuffer:) -> MTLTexture?` that delegates to the existing `render(pair:into:commandBuffer:)`. New constants `senderOutputWidth = 1920`, `senderOutputHeight = 1080`. The pipeline cache (keyed on `(SourceKind, targetPixelFormat)`) automatically grows a `.bgra8Unorm` pipeline on first sender frame. `Metal/SenderPipeline.swift` is the orchestrator — owns the `NDISender` + `UYVYEncoder` + a 3-deep ring of `.shared` UYVY MTLBuffers (sized `bytesPerRow * height = 1920*2*1080`). `send(pair:compositor:)` allocates a fresh command buffer on the shared command queue (independent of the on-screen buffer so screen present and network send never block each other), runs `compositor.renderForSender → encoder.encode`, and on `addCompletedHandler` wraps the `.shared` buffer's `contents()` in a `Data(bytesNoCopy:length:deallocator:.none)` — the SDK's synchronous send copies into its internal queue before the closure returns, so the bytes-no-copy lifetime is exactly the call duration. Frame rate hardcoded to 60p (60000/1000) per issue v1 contract; PRD's source-rate matching is slice #14. `start()` re-`-startWithName:groups:` if not running so scene-phase transitions are recoverable. `Metal/MetalPreviewView.swift` adds an optional `senderPipeline: SenderPipeline?` parameter; the Coordinator's `attach(...)` hooks `pairer.onTick` to (a) cache + redraw the MTKView and (b) call `senderPipeline.send(pair:compositor:)`. Both run on the same main-thread tick but on independent command buffers. `ContentView.swift` constructs the `SenderPipeline` alongside the compositor (default stream name `"Stereo Preview"`, groups `"Public"`), threads it through `MetalPreviewView`, and binds `.onChange(of: scenePhase)` to `senderPipeline.start() / .stop()`. `stereondiTests/UYVYEncoderTests.swift` runs four solid-color tests (white → Y=235 / chroma=128, black → Y=16 / chroma=128, red → Y≈63 Cb≈102 Cr≈240, blue → Y≈32 Cb≈240 Cr≈118), all gated on `MTLCreateSystemDefaultDevice() != nil` so Linux CI exits cleanly via `Issue.record`. ±1 LSB tolerance per channel.
**Deferred / HITL:** Build + sign + run on physical iPad with two live NDI sources is HITL — the manual integration tests ("Studio Monitor on a separate Mac shows the SbS composite at correct resolution / framerate"; "Meta Quest with NDI Theater shows the SbS stream as 3D"; "no frame-rate drop on iPad while sending") all require a Mac + iPad + Quest in the same LAN and are not runnable on this CI VM. Stream name + groups are hardcoded for v1 (slice #10 makes them editable from Settings). Source-rate matching for the output stream is slice #14 territory — v1 advertises 60p regardless of actual source rate, which is what the PRD's "v1 always outputs 1920×1080 SbS" allows. Async `NDIlib_send_send_video_async_v2` was rejected for v1 in favor of the simpler synchronous path; revisit if the GPU completion → CPU send hop turns out to add measurable latency. The 3-deep UYVY ring is belt-and-suspenders insurance — the synchronous send copies internally so 1-deep would suffice, but the cost of the extra two buffers (~8 MB total) is irrelevant. Sender lifecycle is wired to `scenePhase` via `ContentView`'s `.onChange`; the existing `stereondiApp.swift` scene-phase NDIRuntime refcount is independent and unchanged.

## Slice #6 — Convergence + per-eye HIT controls (sub-pixel) (branch `cursor/issue-6-hit-controls-2c32`)
**In:** Core feature of the app — HIT alignment math implemented end-to-end through the shader, the bottom bar UI, and the on-preview gesture overlay. `Models/AlignmentMath.swift` is the pure-function home of the math (`clampHIT(±400)`, `hitToUVOffset(px,srcW)`, `perEyeHIT(conv,leftFine,rightFine) → (-conv/2+leftFine, +conv/2+rightFine)`); imports only Foundation so it's testable on any platform. `Models/AlignmentState.swift` is the `@MainActor @Observable` central knob holder — `convergence`, `leftFineHIT`, `rightFineHIT` (all clamped silently on every set), computed `leftHIT`/`rightHIT` (read fresh per frame by the compositor), `resetAll()`, `nudgeConvergence(by:)`. Sign convention documented in the file header: positive convergence shifts the eyes oppositely (left HIT goes negative, right HIT goes positive — reduces horizontal disparity, "objects come out of the screen"). `Metal/Compositor.metal` adds a per-side `AlignmentUniforms` (`u_min`, `u_max`, `reserved_crop`, `_padding` — slice #7 repurposes `reserved_crop` as a mode flag); a shared `remapped_source_uv()` helper applies the HIT-translated + auto-crop window to both BGRA and UYVY fragments via `src_u = u_min + dst_u * (u_max - u_min)`; bilinear filter on the existing `filter::linear, address::clamp_to_edge` sampler delivers PRD-spec'd 0.1 px sub-pixel precision for free; out-of-[0,1] source UV returns black (defensive for slice #7's OFF mode). `Metal/StereoCompositor.swift` adds `struct AlignmentUniforms` matching the metal layout, `static func alignmentUniforms(forSideHITPixels:otherSideHITPixels:sourceWidthPixels:otherSourceWidthPixels:)` that computes `(u_min, u_max)` per eye using the common-region crop `commonAbsUV = max(|leftHitUV|, |rightHitUV|)` with each eye's own source width, and changes `render(...)` + `renderForSender(...)` to require an `alignment: AlignmentState` argument (read fresh per frame; no caching). When both HITs are zero the uniforms collapse to `(uMin=0, uMax=1)` so the slice #4 zero-HIT golden reference renders byte-identical. `Metal/MetalPreviewView.swift` and `Metal/SenderPipeline.swift` thread the same `AlignmentState` reference into both render paths, so a slider drag or a two-finger pan reflects on the iPad screen and in the NDI output within one frame. `Views/BottomBar.swift` is the always-visible hero bar — Reset, ±1 px / ±0.1 px nudge buttons flanking the convergence Slider, integer-px numeric readout (`Convergence: NN px`), and a `▼ Per-eye fine` disclosure that reveals two ±400 px sliders. `Views/PreviewGestureOverlay.swift` is layered between the MTKView and the chrome bars in `ContentView`'s ZStack: a UIKit `UIPanGestureRecognizer` with `min/maxNumberOfTouches = 2` adjusts `convergence` at ~0.5 px per logical drag-point (PRD: ~1 px / ~2 points); a SwiftUI `MagnificationGesture` clamps `zoom ∈ [1.0, 2.0]` (applied as a `.scaleEffect(zoom)` on the MTKView — visual-only, no Metal pipeline change); a `TapGesture(count: 2)` resets zoom to 1.0; the pan recognizer's delegate returns `true` for simultaneous recognition so a true pinch on the same overlay still resolves. `ContentView.swift` owns one `AlignmentState` + a `@State zoom: CGFloat = 1.0`, mounts the gesture overlay between the preview and the bars, and adds the `BottomBar` to the bottom of its `VStack`. Tests: `AlignmentMathTests.swift` covers clamping (±400, 0, in-range pass-through), `hitToUVOffset` (1920-px source, sub-pixel input, divide-by-zero guard), `perEyeHIT` invariants (zero/non-zero conv with/without fine), and `StereoCompositor.alignmentUniforms` (zero-HIT → (0,1), sub-pixel symmetric, asymmetric → common-abs crop). `AlignmentStateTests.swift` exercises the full Observable surface (clamping on all three knobs, opposite-shift convergence math, fine adds, reset, sub-pixel nudge precision within IEEE-754 tolerance). `StereoCompositorGoldenTests.swift` factored into a shared `renderAndCompare(named:convergence:)` harness running five tests at convergence 0 / +50 / -50 / +200 / +0.3.
**Deferred / HITL:** Reference PNGs `sbs_hit_p50.png`, `sbs_hit_n50.png`, `sbs_hit_p200.png`, `sbs_hit_p0_3.png` are not committed for the same Linux-no-Metal reason as slice #4's `sbs_zero_hit_gradient.png` — the user must run `STEREONDI_UPDATE_GOLDENS=1 xcodebuild test -scheme stereondi …` once on a Mac to seed all five (see `stereondiTests/Goldens/README.md`). Build + run on physical iPad to verify HIT slider + two-finger pan + pinch-to-zoom on the live preview is HITL. Auto-crop is the only mode this slice (per PRD: "auto-crop on by default for this slice"); slice #7 adds the OFF toggle by setting per-side `commonAbsUV = 0` so the existing shader's out-of-[0,1]→black path lights up. Per-eye fine sliders are not visually disabled when their effect would clip past ±400 px; the AlignmentState clamps silently, which the issue explicitly endorses. Zoom is implemented as a SwiftUI `.scaleEffect(zoom)` on the MTKView (visual-only); slice #11 may bake it into the shader if the 2× UI scale ever produces visible aliasing. Persistence of HIT state across launches is slice #10 (`SessionStore`). The new TopBar mode segmented control / EmptyState polish remains its own slices' work.

## Slice #7 — Crop toggle (auto-crop common region vs full-frame with black bars) (branch `cursor/issue-7-crop-toggle-2c32`)
**In:** Crop-mode toggle wired end-to-end. `Models/AlignmentState.swift` adds the `CropMode` enum (`.auto` / `.off`, `String`-raw, `CaseIterable`, `Sendable`), an `@Observable var cropMode: CropMode = .auto` stored property, and a `toggleCrop()` helper. `resetAll()` intentionally does NOT reset `cropMode` (operators frequently reset HIT mid-take while keeping the crop preference) — covered by a unit test. `Models/AlignmentMath.swift` factors the slice-#6 inline auto-crop math into a pure `uvWindow(hitPixels:otherHitPixels:sourceWidthPixels:otherSourceWidthPixels:cropMode:) -> (uMin, uMax)` helper that handles both modes: `.auto` returns `(commonAbsUV + hitUV, (1 − commonAbsUV) + hitUV)` (slice-#6 math); `.off` returns exactly `(hitUV, hitUV + 1)` so the shader's existing `uv_outside_source` check produces the missing-edge black bars without any shader-side branching. The geometry of the per-eye output rect is identical in both modes (the aspect-fit half), only the sampling window differs — so OFF mode preserves source pixel-aspect across the destination half, which is the whole point. `Metal/StereoCompositor.swift` renames `AlignmentUniforms.reservedCrop` → `cropModeFlag` (Swift) and the metal struct's `reserved_crop` → `crop_mode_flag` to match; `alignmentUniforms(...)` now takes a `cropMode:` argument and delegates to `AlignmentMath.uvWindow(...)`; `render(...)` snapshots `alignment.cropMode` once per frame and threads it through `drawSide(...)` along with the per-eye HITs. The shader itself is unchanged — the CPU does the right `(u_min, u_max)` math for the active mode and the existing out-of-[0,1] black branch lights up only in OFF. `cropModeFlag` is informational only (0 = auto, 1 = off), wired through for future shader work that may need to specialize on the mode. `Views/BottomBar.swift` adds a segmented `Picker("Crop", selection: $alignment.cropMode) { Auto / Off }` inside the existing per-eye-fine disclosure panel (separate row beneath the two fine sliders), with an accessibility label/value describing the active mode. Tests: `AlignmentMathTests.swift` adds six `uvWindow` cases (auto symmetric HIT → outside edge at exactly 1.0; off symmetric HIT → window width stays at 1; auto with mismatched per-eye widths uses each eye's own normalization for HIT and the larger normalized of the two for the common crop; auto and off both collapse to (0, 1) at zero HIT; off with negative HIT pushes uMin below 0 producing a left-edge black bar) plus a `uniformsCropOffSetsFlag` test verifying the GPU-uniform packing path encodes the mode flag and OFF window correctly. `AlignmentStateTests.swift` adds `cropToggle` (default `.auto`, `toggleCrop()` flips, second toggle returns) and `resetAllPreservesCropMode`. `StereoCompositorGoldenTests.swift` renames `sbs_hit_p50.png` → `sbs_hit_p50_crop_auto.png` (no rendering change — slice #6's render was implicitly auto) and adds `sbs_hit_p50_crop_off.png` at the same +50 convergence with `cropMode = .off`; the harness gains a `cropMode: CropMode = .auto` parameter so the existing four other goldens render unchanged. `stereondiTests/Goldens/README.md` updated with the renamed + new golden names.
**Deferred / HITL:** Reference PNG `sbs_hit_p50_crop_off.png` is the one new HITL seeding item — same Linux-no-Metal constraint, same `STEREONDI_UPDATE_GOLDENS=1 xcodebuild test …` workflow as slices #4 and #6. The slice-#6 `sbs_hit_p50.png` should be deleted from the live test bundle after re-seeding (the test now looks for `sbs_hit_p50_crop_auto.png`); the README calls this out. Build + run on physical iPad to verify the bottom-bar disclosure picker switches between AUTO (clean image at non-zero HIT) and OFF (visible black bar on the inside edge of one half) is HITL. The crop-mode flag in `AlignmentUniforms` is wired through but unread by the current shader — it's there as a no-cost hook for future per-mode specialization (e.g. anaglyph). Persistence of `cropMode` across launches is slice #10 (`SessionStore`). NDI output reflects the toggle automatically because both render paths share the single `AlignmentState` reference threaded by slice #6.

## Slice #8 — iPad anaglyph mode + two-pipeline split + channel test + swap-eyes (branch `cursor/issue-8-anaglyph-two-pipeline-2c32`)
**In:** Compositor split into two final-stage render entry points sharing all upstream stages (per-eye `CVMetalTexture` cache + `AlignmentMath` uniform calc), plus two new screen-only modes. `Models/AlignmentState.swift` adds `enum ScreenMode: String, CaseIterable, Sendable { case sbs, anaglyph, channelTest }` (default `.sbs`), `@Observable var screenMode: ScreenMode`, and `@Observable var swapEyes: Bool = false`. `resetAll()` intentionally preserves `screenMode` and `swapEyes` (alongside the slice-#7 `cropMode` carve-out) — operators reset HIT mid-take but keep their preview-mode and glasses-orientation choices; covered by `resetAllPreservesScreenMode` / `resetAllPreservesSwapEyes` unit tests. `Metal/Compositor.metal` adds: a fullscreen-quad vertex (`fullframe_vertex`); `fragment_anaglyph` — pure-luma BT.709 red/cyan with a single combined draw that takes both source textures plus a per-side `AnaglyphSideUniforms` carrying `(dst-subrect-in-full-UV, uMin, uMax, decodeMode)`, so the shader handles BGRA-BGRA, BGRA-UYVY, and UYVY-UYVY source pairs with one pipeline (decode_mode flag flips between full-range BGRA and BT.709-limited UYVY decode in-shader); `fragment_channeltest` — `position.x < 0.5 → solid (1, 0, 0, 1)`, else `(0, 1, 1, 1)`, sources unbound. `Metal/StereoCompositor.swift` is rewritten around the split: `renderScreen(pair:alignment:into:commandBuffer:)` switches on `alignment.screenMode` and dispatches to `renderSbS` / `renderAnaglyph` / `renderChannelTest`; `renderForSender(...)` is hard-wired to `renderSbS` regardless of `screenMode` (PRD user story 17 + 30 — Quest viewer's stream stays uninterrupted while the operator iterates between alignment views); the legacy `render(...)` is preserved as an `@available(*, deprecated, renamed: "renderScreen…")` wrapper so any external caller still compiles. New nested types `Mode { .sbs, .anaglyph, .channelTest }`, `AnaglyphSideUniforms`, `AnaglyphUniforms`, and `ResolvedSource` (the shared per-eye CVMetalTexture cache lookup is factored out so SbS and anaglyph share one resolution path). The pipeline cache is rekeyed `(SourceKind?, Mode, targetPixelFormat)` — `SourceKind` is nil for `.anaglyph` (one shader handles all source-format permutations) and `.channelTest` (no sampling). Swap-eyes is implemented at uniform-binding time (CPU-side swaps which `ResolvedSource` goes into the shader's `red_source` vs `cyan_source` slot — no shader branch). Anaglyph aspect-fits each source over the FULL output rect via `aspectFitFullFrameUVRect` (16:9 source on 16:9 target → edge-to-edge; 4:3 source → pillar-boxed). Single-source anaglyph: the missing eye's `dst-subrect` is zero-area, so the shader's bounds check zeroes that side's luma contribution. `Metal/MetalPreviewView.swift` Coordinator now calls `compositor.renderScreen(...)` (was `compositor.render(...)`); `Metal/SenderPipeline.swift` is unchanged — it already calls `renderForSender(...)` which is the always-SbS sender path. `Views/BottomBar.swift` gets a temporary `#if DEBUG` `Menu` (ladybug icon) carrying a `Picker("Screen mode (debug)", selection: $alignment.screenMode)` over `ScreenMode.allCases` and a `Toggle("Swap eyes (debug)", isOn: $alignment.swapEyes)` so QA can verify the new shaders before slice #9 wires the proper UI; removing the `#if DEBUG` block in slice #9 leaves the rest of the bar unchanged. Tests: `AlignmentStateTests.swift` adds `screenModeDefaultsToSbS`, `screenModeTransitionsAcrossAllCases`, `swapEyesDefaultsFalse`, `swapEyesToggles`, `resetAllPreservesScreenMode`, `resetAllPreservesSwapEyes`. `StereoCompositorGoldenTests.swift` adds `anaglyphZeroHitGradient` (→ `anaglyph_zero_hit_gradient.png`), `anaglyphSwappedZeroHitGradient` (→ `anaglyph_swapped_zero_hit_gradient.png`, swap-eyes ON), and `channelTest` (→ `channel_test.png`, sources nil); the harness gains `screenMode` + `swapEyes` parameters and a `renderAndCompareNoSources(...)` variant for the channel-test case. The harness now drives `compositor.renderScreen(...)` instead of the deprecated `render(...)` so all six existing SbS goldens validate the new shared-upstream path produces byte-identical output. `StereoCompositorPipelineTests.swift` is the new sender-vs-screen separation Metal test (gated on `MTLCreateSystemDefaultDevice() != nil` so Linux CI exits cleanly): with `screenMode = .anaglyph` and a red-left/blue-right gradient pair, asserts (a) `renderForSender(...)` produces an SbS output (left-half pixel is red-dominant, right-half pixel is blue-dominant), (b) `renderScreen(...)` produces an anaglyph composite (pure cyan in the green+blue channels — `G == B`).
**Deferred / HITL:** Three new reference PNGs (`anaglyph_zero_hit_gradient.png`, `anaglyph_swapped_zero_hit_gradient.png`, `channel_test.png`) need to be seeded on a Mac via `STEREONDI_UPDATE_GOLDENS=1 xcodebuild test …` — same Linux-no-Metal constraint as the slice-#4/#6/#7 goldens; `stereondiTests/Goldens/README.md` lists them explicitly. The six existing SbS goldens are expected to continue passing byte-identically against the new shared-upstream code path; if they drift, the shared-upstream refactor regressed something (this is the slice's "sanity check" item per the issue). Build + run on physical iPad to verify the debug ladybug menu flips screen mode + swap-eyes is HITL; the manual integration test is "iPad in anaglyph while Quest stream stays SbS in NDI Studio Monitor" — confirms both pipelines are truly independent. The TopBar segmented mode-switch UI is **explicitly slice #9** (the AC item "iPad screen mode changeable via a debug-only toggle (UI hookup is in the next slice)" — the ladybug menu is the temporary substitute, replaceable by removing the `#if DEBUG` block). Persistence of `screenMode` and `swapEyes` across launches is slice #10 (`SessionStore`). True shared *intermediate textures* (a pre-final-stage SbS reference texture computed once and consumed by both pipelines) is a v1.1 optimization the issue explicitly carves out — at slice-#8 scope "shared upstream" means the per-eye `CVMetalTexture` cache lookup and the alignment-uniform calculation, not a shared rendered intermediate. The anaglyph shader's per-side `decodeMode` flag handles BGRA-BGRA / BGRA-UYVY / UYVY-UYVY source-format permutations in a single pipeline, trading a runtime `if (decode_mode == 1)` branch for not exploding the pipeline cache on source-pair format; revisit if profiling shows this branch costs more than the extra pipeline-state objects would.

## Slice #9 — iPad screen mode switcher UI (SbS / Anaglyph / Channel Test) (branch `cursor/issue-9-mode-switcher-2c32`)
**In:** Operator-facing UI for the slice-#8 split. `Views/TopBar.swift` now takes `@Bindable var alignment: AlignmentState` alongside the existing source-selection / discovery args; the bar's middle gains a SwiftUI `Picker("Screen mode", selection: $alignment.screenMode).pickerStyle(.segmented)` with three short labels — "SbS", "Anaglyph", "Channel Test" — flanked by flexible spacers so the source pickers sit hard-left and the gear sits hard-right. The picker is bound directly to the `@Observable` `screenMode`, so a tap propagates into `StereoCompositor.renderScreen(...)` on the next pairer tick (≤ one frame, no caching anywhere — `MetalPreviewView`'s coordinator reads `alignment.screenMode` fresh inside the tick callback per slice #8). When `alignment.screenMode == .anaglyph`, a conditional `Menu` ("ellipsis.circle" SF Symbol, `.borderlessButton` style) appears between the segmented control and the gear carrying a `Toggle("Swap eyes", isOn: $alignment.swapEyes)`; in the other two modes the `Menu` is not mounted at all (rather than `.disabled` or `.opacity(0)`) so the bar physically narrows back to SbS/channel-test layout — consistent with the `AlignmentState` contract that swap-eyes is a no-op outside anaglyph. The NDI-output pipeline is provably untouched: `StereoCompositor.renderForSender(...)` is hard-wired to SbS regardless of `screenMode`, and `SenderPipeline` calls only that entry point — so the segmented control is purely a screen-pipeline knob (PRD user stories 17 + 30, manual-verifiable via NDI Studio Monitor on a separate Mac). `Views/BottomBar.swift` drops the slice-#8 `#if DEBUG` ladybug `Menu` (and its `Image(systemName: "ladybug")`); the `mainRow` `HStack` now just ends after the per-eye-fine disclosure button. The disclosure content (per-eye sliders + crop picker) is unchanged. `ContentView.swift` threads `alignment` into `TopBar(...)` as the second argument (matching the existing `BottomBar(alignment:)` pattern) — no other ContentView changes needed. Tests: `AlignmentStateTests.swift` adds two slice-#9-specific cases — `switchingToAnaglyphPreservesSwapEyes` (set swap-eyes ON, then enter anaglyph; assert the value survives) and `swapEyesSurvivesScreenModeRoundTrip` (anaglyph → channel-test → sbs → anaglyph round trip with swap-eyes ON throughout); the slice-#8 default/transition/preserve tests cover the rest of the surface as the issue's "review and only add if missing" guidance prescribes. No Metal / shader / NDI bridge changes; no project-file changes (file-system-synchronized group picks up the test additions). Verified with `rg "ladybug" stereondi/` returning zero hits.
**Deferred / HITL:** Build + run on physical iPad confirming the segmented control flips the iPad-screen pipeline within one frame is HITL (no Xcode in CI VM); the manual integration test is "switch SbS → Anaglyph → Channel Test on the iPad while NDI Studio Monitor on a separate Mac shows the SbS stream uninterrupted" — this is the AC item "NDI output unaffected by screen-mode changes (verified via NDI Studio Monitor)". TopBar layout on 11" iPad landscape (1194 × 834 pt) is HITL — the layout uses two flexible `Spacer(minLength: 8)` regions, a `.frame(maxWidth: 320)` cap on the segmented control, and `.lineLimit(1) + .truncationMode(.middle)` on the source-button labels so long NDI source names truncate cleanly rather than push the gear off-screen; if a real-world source name still overflows, drop the picker's `.frame(maxWidth:)` cap or shorten the segment labels (the in-code constants are the only knobs needed). Dark-mode rendering of the `.segmented` picker style works by default with the app's `.preferredColorScheme(.dark)` (HITL-verified on first build). Persistence of `screenMode` and `swapEyes` across launches remains slice #10 (`SessionStore`). The Settings gear in the TopBar is still a disabled placeholder; slice #10 wires it. The overflow `Menu`'s SF Symbol is `ellipsis.circle` (the issue says "overflow ⋯") — swap to a different glyph in slice #13's auto-hide / chrome polish pass if the operator finds it ambiguous.
